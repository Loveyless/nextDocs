---
title: 页面和布局
description: 使用 Pages Router 创建你的第一个页面和共享布局。
---

Pages Router 有一个基于文件系统的路由器，构建在页面的概念之上。

当文件添加到 `pages` 目录时，它会自动作为路由可用。

在 Next.js 中，**页面**是从 `pages` 目录中的 `.js`、`.jsx`、`.ts` 或 `.tsx` 文件导出的 [React 组件](https://react.dev/learn/your-first-component)。每个页面根据其文件名与路由相关联。

---
title: 自动静态优化
description: Next.js 在可能的情况下自动将你的应用优化为静态 HTML。在这里了解它的工作原理。
---

如果页面没有阻塞数据需求，Next.js 会自动确定页面是静态的（可以预渲染）。这个判断是通过页面中不存在 `getServerSideProps` 和 `getInitialProps` 来做出的。

此功能允许 Next.js 发出包含**服务器渲染和静态生成页面**的混合应用。

> **了解更多**：静态生成的页面仍然是响应式的。Next.js 将在客户端为你的应用注水，使其具有完全交互性。

---
title: getServerSideProps
description: 使用 `getServerSideProps` 在每个请求上获取数据。
---

`getServerSideProps` 是一个 Next.js 函数，可用于在请求时获取数据并渲染页面内容。

## 示例

你可以通过从页面组件导出 `getServerSideProps` 来使用它。下面的示例显示了你如何在 `getServerSideProps` 中从第三方 API 获取数据，并将数据作为 props 传递给页面：

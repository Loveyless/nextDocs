---
title: getStaticPaths
description: 使用 `getStaticPaths` 获取数据并生成静态页面。了解更多关于 Next.js 中此数据获取 API 的信息。
---

如果页面有[动态路由](/docs/pages/building-your-application/routing/dynamic-routes)并使用 `getStaticProps`，它需要定义要静态生成的路径列表。

当你从使用动态路由的页面导出一个名为 `getStaticPaths`（静态站点生成）的函数时，Next.js 将静态预渲染由 `getStaticPaths` 指定的所有路径。

```tsx filename="pages/repo/[name].tsx" switcher

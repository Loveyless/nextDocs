---
title: 可访问性
description: Next.js 的内置可访问性功能。
---

Next.js 团队致力于让 Next.js 对所有开发者（及其最终用户）都可访问。通过默认向 Next.js 添加可访问性功能，我们旨在让网络对每个人都更加包容。

## 路由公告

在服务器渲染的页面之间转换时（例如使用 `<a href>` 标签），屏幕阅读器和其他辅助技术会在页面加载时宣布页面标题，以便用户了解页面已更改。
